<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.duck.petcareproject.mapper.CommunityPostMapper">
		
		<!-- 검색/정렬 포함 게시글 목록 (페이징) -->
		<select id="selectPostsPagingSearch" resultType="CommunityPost">
			SELECT
				p.post_seq,
				p.user_seq,
				m.user_name AS userName,
				p.category,
				p.title,
				p.content,
				p.view_count,
				p.created_at,
				p.updated_at,
				(
					SELECT COUNT(*)
					FROM communication_comment c
					WHERE c.post_seq = p.post_seq
				) AS commentCount
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
		
			<where>
				<if test="category != null and category != ''">
					p.category = #{category}
				</if>
		
				<if test="keyword != null and keyword != ''">
					AND (
						p.title LIKE CONCAT('%', #{keyword}, '%')
						OR p.content LIKE CONCAT('%', #{keyword}, '%')
						OR m.user_name LIKE CONCAT('%', #{keyword}, '%')
					)
				</if>
			</where>
		
			<choose>
				<when test="sort == 'old'">
					ORDER BY p.post_seq ASC
				</when>
				<otherwise>
					ORDER BY p.post_seq DESC
				</otherwise>
			</choose>
		
			LIMIT #{limit} OFFSET #{offset}
		</select>
		
		<!-- 검색 포함 게시글 수 -->
		<select id="countPostsSearch" resultType="int">
			SELECT COUNT(*)
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
			<where>
				<if test="category != null and category != ''">
					p.category = #{category}
				</if>
		
				<if test="keyword != null and keyword != ''">
					AND (
						p.title LIKE CONCAT('%', #{keyword}, '%')
						OR p.content LIKE CONCAT('%', #{keyword}, '%')
						OR m.user_name LIKE CONCAT('%', #{keyword}, '%')
					)
				</if>
			</where>
		</select>

		<!-- 게시글 상세 조회 (작성자 이름 + 댓글 수 포함) -->
		<select id="selectPostById" resultType="CommunityPost">
			SELECT
				p.post_seq,
				p.user_seq,
				m.user_name AS userName,
				p.category,
				p.title,
				p.content,
				p.view_count,
				p.created_at,
				p.updated_at,
				(
					SELECT COUNT(*)
					FROM communication_comment c
					WHERE c.post_seq = p.post_seq
				) AS commentCount
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
			WHERE p.post_seq = #{postSeq}
		</select>

		
		<!-- 조회수 증가 -->
		<update id="incrementViewCount">
			UPDATE communication_post
			SET view_count = view_count + 1
			WHERE post_seq = #{postSeq}
		</update>
		
		<!-- 내 게시글 조회 (권한 체크) -->
		<select id="selectPostByIdAndUser" resultType="CommunityPost">
			SELECT *
			FROM communication_post
			WHERE post_seq = #{postSeq}
				AND user_seq = #{userSeq}
		</select>
		
		<!-- 게시글 수정 (내 글만) -->
		<update id="updatePostByIdAndUser" parameterType="CommunityPost">
			UPDATE communication_post
			SET
				category = #{category},
				title = #{title},
				content = #{content}
			WHERE post_seq = #{postSeq}
				AND user_seq = #{userSeq}
		</update>
		
		<!-- 게시글 삭제 (내 글만) -->
		<delete id="deletePostByIdAndUser">
			DELETE FROM communication_post
			WHERE post_seq = #{postSeq}
				AND user_seq = #{userSeq}
		</delete>
		
		<!-- 게시글 삭제 (관리자: 작성자 조건 없음) -->
		<delete id="deletePostById">
			DELETE FROM communication_post
			WHERE post_seq = #{postSeq}
		</delete>
		
		<!-- 게시글 등록 -->
		<insert id="insertPost" parameterType="CommunityPost" useGeneratedKeys="true" keyProperty="postSeq">
			INSERT INTO communication_post (user_seq, category, title, content)
			VALUES (#{userSeq}, #{category}, #{title}, #{content})
		</insert>
		
		<!-- 전체 게시글 목록 (카테고리 선택 시 필터) -->
		<select id="selectPostsPaging" resultType="CommunityPost">
			SELECT
				p.post_seq,
				p.user_seq,
				m.user_name AS userName,
				p.category,
				p.title,
				p.content,
				p.view_count,
				p.created_at,
				p.updated_at,
				(
					SELECT COUNT(*)
					FROM communication_comment c
					WHERE c.post_seq = p.post_seq
				) AS commentCount
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
		
			<where>
				<if test="category != null and category != ''">
					p.category = #{category}
				</if>
			</where>
		
			ORDER BY p.post_seq DESC
			LIMIT #{limit} OFFSET #{offset}
		</select>

		
		<!-- 전체 게시글 전체 수 (페이징용 count) -->
		<select id="countPosts" resultType="int">
			SELECT COUNT(*)
			FROM communication_post p
			<where>
				<if test="category != null and category != ''">
					p.category = #{category}
				</if>
			</where>
		</select>
		
		<!-- userSeq 기준 오늘의 게시글 페이지 목록 (페이징) - 대시보드용 -->
		<select id="selectTodayPostsByUserPaging" resultType="CommunityPost">
			SELECT
				p.post_seq,
				p.user_seq,
				m.user_name AS userName,
				p.category,
				p.title,
				p.content,
				p.view_count,
				p.created_at,
				p.updated_at,
				(
					SELECT COUNT(*)
					FROM communication_comment c
					WHERE c.post_seq = p.post_seq
				) AS commentCount
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
			WHERE p.user_seq = #{userSeq}
				AND p.created_at >= CURDATE()
				AND p.created_at &lt; CURDATE() + INTERVAL 1 DAY
			ORDER BY p.created_at DESC
			LIMIT #{limit} OFFSET #{offset}
		</select>
		
		<!-- userSeq 기준 오늘의 게시글 전체 수 -->
		<select id="countTodayPostsByUser" resultType="int">
			<![CDATA[
				SELECT COUNT(*)
				FROM communication_post p
				WHERE p.user_seq = #{userSeq}
					AND p.created_at >= CURDATE()
					AND p.created_at < CURDATE() + INTERVAL 1 DAY
			]]>
		</select>
		
		<!-- ========================= -->
		<!-- 관리자 대시보드: 목록(검색/정렬/페이징) -->
		<!-- ========================= -->
		<select id="selectPostsAdminPaging" resultType="CommunityPost">
			SELECT
				p.post_seq,
				p.user_seq,
				m.user_name AS userName,
				p.category,
				p.title,
				p.content,
				p.view_count,
				p.created_at,
				p.updated_at,
				(
					SELECT COUNT(*)
					FROM communication_comment c
					WHERE c.post_seq = p.post_seq
				) AS commentCount
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
		
			<where>
				<if test="category != null and category != ''">
					p.category = #{category}
				</if>
		
				<if test="keyword != null and keyword != ''">
					AND
					<choose>
						<when test="type == 'name'">
							m.user_name LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'id'">
							m.user_id LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'title'">
							p.title LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'content'">
							p.content LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<otherwise>
							(
								p.title LIKE CONCAT('%', #{keyword}, '%')
								OR p.content LIKE CONCAT('%', #{keyword}, '%')
								OR m.user_name LIKE CONCAT('%', #{keyword}, '%')
								OR m.user_id LIKE CONCAT('%', #{keyword}, '%')
							)
						</otherwise>
					</choose>
				</if>
			</where>
		
			<choose>
				<when test="sort == 'old'">
					ORDER BY p.post_seq ASC
				</when>
				<otherwise>
					ORDER BY p.post_seq DESC
				</otherwise>
			</choose>
		
			LIMIT #{limit} OFFSET #{offset}
		</select>
		
		<select id="countPostsAdmin" resultType="int">
			SELECT COUNT(*)
			FROM communication_post p
			JOIN member m ON m.user_seq = p.user_seq
		
			<where>
				<if test="category != null and category != ''">
					p.category = #{category}
				</if>
		
				<if test="keyword != null and keyword != ''">
					AND
					<choose>
						<when test="type == 'name'">
							m.user_name LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'id'">
							m.user_id LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'title'">
							p.title LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<when test="type == 'content'">
							p.content LIKE CONCAT('%', #{keyword}, '%')
						</when>
						<otherwise>
							(
								p.title LIKE CONCAT('%', #{keyword}, '%')
								OR p.content LIKE CONCAT('%', #{keyword}, '%')
								OR m.user_name LIKE CONCAT('%', #{keyword}, '%')
								OR m.user_id LIKE CONCAT('%', #{keyword}, '%')
							)
						</otherwise>
					</choose>
				</if>
			</where>
		</select>

</mapper>
